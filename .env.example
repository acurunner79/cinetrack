# Get these from https://www.themoviedb.org/settings/api
# after creating a free TMDb account

# v3 API key (used for most endpoints + auth flow)
VITE_TMDB_API_KEY=your_api_key_here

# v4 Read Access Token (Bearer token — used for v4 endpoints)
VITE_TMDB_READ_TOKEN=your_read_access_token_here
